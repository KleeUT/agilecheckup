name: deploy-website
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2
      - name: Publish
        uses: cloudflare/wrangler-action@1.3.0
        with:
          preCommands: yarn deploy
          publish: false # use deploy script to deploy
          apiToken: ${{ secrets.CF_API_TOKEN }}
